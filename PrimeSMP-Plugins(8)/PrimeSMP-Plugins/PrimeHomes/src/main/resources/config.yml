# PrimeHomes configuration

# Default home limit for players with no primehomes.limit.* permission.
default-home-limit: 1

# Seconds of standing still required before /home triggers (0 = instant).
warmup-seconds: 3

# Seconds between uses of /home (per player).
cooldown-seconds: 5
